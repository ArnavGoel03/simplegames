# Studio 0.5.1 release record

Live at `https://glasstablegames.com`. The provider confirmed 100% traffic on
the verified Worker at 2026-09-13 19:49:57 UTC.

Candidate source: `007ca22d0e84afb119b3dcdf62cd2c356eac6764`.
Worker: `4f6f1da4-aebf-4817-b5a0-281bbd15eaf2`.
Historical preview: `https://4f6f1da4-glasstablegames.goelhome.workers.dev`.
Preview access is now disabled; this URL records the verification target.

The startup bootstrap can run while an earlier stylesheet is stalled. Recovery
waits for the configured hydration grace before fallback script probes, while
confirmed CSS/script errors remain actionable. Shared OpenNext configuration
disables the installed adapter's incompatible prefetch-inlining branch. The
Games link navigates directly to its existing fragment. Pastel materials,
checked backgrounds and the responsive gallery remain.

The 24.129-second gate passed 154 Vitest tests, 33 canonical Node tests,
TypeScript 7 and zero-warning lint. The production build was warning-free.
The release receipt binds source fingerprint, generated output, immutable
upload, authenticated browser artifacts and measured performance policy.

| Engine | Successful run | Artifact | Startup median ms | Completed Games navigation median ms | Decoded initial JS/CSS bytes |
| --- | --- | --- | ---: | ---: | ---: |
| Chromium | 34778527485 | 10324114128 | 543.8000000000466 | 46.5 | 510982 |
| WebKit | 34777867969 | 10323833922 | 490 | 132 | 510982 |
| Limit | | | 750 | 400 | 655360 |

Both artifacts identify the exact candidate source and Worker above. Chromium
used verification workflow source `1bb5a3cc80334b89887665bbfddf2fd9adb1be88`;
WebKit used `007ca22d0e84afb119b3dcdf62cd2c356eac6764`. Responsive, entry,
startup, network, failed-CSS, offline and update checks all passed.

The completed-navigation metric replaces an invalid early click timer that
could stop before the live Next link scrolled. Its live Chromium/WebKit
medians were 245.89999999999418/190 ms. The existing 1.5x rule, rounded up to
50 ms, yields 400 ms. No startup or asset allowance changed. Exact corrected
baseline runs, samples and artifact digests are recorded in
`docs/quality/browser-baseline.md`.

Chromium's offline test used the actual candidate with browser-emulated offline
networking. WebKit's offline emulator blocks before dispatching a worker fetch;
an unconditional-response control established that limit. Its passing check
uses the exact candidate worker with an acknowledged offline test environment,
stops the fixture HTTP origin, and proves an uncached request fails. Candidate
online source and styles were verified separately. This is controlled worker
branch coverage, not physical Safari or native offline-toggle certification.
Those physical-device checks remain outstanding.

A fresh production storage round-trip for the added diagnostic startup fields
remains unverified. Neither canonical local game release environment exposed
DATABASE_URL for scoped row readback and cleanup. No synthetic report was sent
or row created by that attempted check; production database health checks pass.

Local evidence: `.audit/quality/gate.json`, `.audit/quality/candidates/studio.json`
and `.audit/quality/releases/studio.json`. Promotion receipt:
`.audit/quality/deployments/studio-1789328990082.json`. The provider deployment
ID is `2125a242-ff22-4e29-bf48-247ebbd5968b`. Rollback preserves the prior 100%
version `8bb33c61-8312-42e1-b2b7-3dce9e5bddf2`.

After promotion, `npm run verify:live` passed 144 checks across 11 pages and
manifest shortcuts. The studio catalogue, runtime policy and PWA source match
the game release. Output is retained in `.audit/verify-live-final.log`.

The live homepage source, all ten startup CSS/JS assets and the service worker
match the certified output exactly. The detector rejected a same-size one-byte
CSS mutation. Evidence: `.audit/quality/live-studio/exact-content.json`.
Temporary preview access was restored to its prior disabled state, with both
`enabled=false` and `previews_enabled=false` confirmed by provider readback in
`.audit/quality/preview-settings/studio-restore-1789329298340.json`.

Supplementary local PR run `34779977061` passed all twelve responsive captures
and startup recovery, but its three cold performance contexts failed network
validation. Playwright 1.63's blocked-worker shim advertises registration while
resolving it with `undefined`; the real worker client consequently reports a
registration fault. Each local diagnostic POST returned 403 and was rejected by
the network gate. The trace contains no failed CSS, JavaScript or RSC request.

The test now removes the service-worker capability only in those intentionally
worker-free performance contexts. Normal visual and lifecycle contexts retain
the real API. Twenty-five focused tests pass, including reproduction through
the actual worker client and controls retaining genuine registration and HTTP
403 failures. Supplementary run `34780722592`, source `d77c26d`, passed the
full gate, local build, twelve responsive captures and three startup-recovery
controls. Artifact `10324548345` confirms zero runtime errors, failed network
requests or synthetic diagnostic POSTs across all five visual/performance
contexts. Only this receipt was updated after that tested source; the deployed
candidate certificates and application bytes are unchanged.
