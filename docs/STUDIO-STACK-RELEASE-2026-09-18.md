# Studio 0.5.3 release

https://glasstablegames.com now serves source
`cfdf6a486095d9e939a29a9adba7e2e9e50f2ea4`, immutable Worker
`a7e23fdd-b31c-4f73-bb38-0e8dba884f7a`, at 100% of production traffic.
Provider deployment `56b15abd-d938-4d9e-a532-56cab0a2847f` was created at
2026-09-17T20:57:49Z. This publishes the already merged stack upgrade and privacy
runtime correction. No pull request was merged during this release.

The proposed `LEGAL-APPROVAL.diff` remains unapplied. Legal components, routes
and registry were compared against prior production `bdd9c16` and have no diff.
That approval holds the proposed wording, not this verified runtime release.
Compression PR19 remains a separate unmerged draft; its candidate was not
promoted and its old timing refusal was not waived.

## Acceptance and public receipts

The isolated source passed the certified local gate: 171 application tests,
33 release-policy tests, native compiler calibration, typecheck and zero-warning
lint. The canonical Cloudflare build passed and certified the uploaded bytes.

| Exact candidate browser | Startup median | Games navigation median | Initial decoded JS/CSS |
| --- | ---: | ---: | ---: |
| Chromium | 385.2 ms | 45.3 ms | 513,005 bytes |
| WebKit | 634 ms | 139 ms | 513,005 bytes |
| Unchanged limit | 750 ms | 400 ms | 655,360 bytes |

Successful runs are [Chromium 35273378467](https://github.com/ArnavGoel03/simplegames/actions/runs/35273378467)
and [WebKit 35273380596](https://github.com/ArnavGoel03/simplegames/actions/runs/35273380596).
The release validator verified their candidate-evidence artifacts, source,
immutable version, generated bytes and historical baselines. Required startup,
responsive, network, failed-CSS, entry, offline and update checks all passed.
WebKit offline evidence retains its documented controlled-worker limitation;
it is not physical-device or native offline-toggle certification.

The first dispatch pair, 35273077305/35273079815, rejected an invalid baseline-only
`SITE_FILTER` input before navigation. Corrected dispatches omitted that input.
No measured timing failure was retried. Lesson: exact candidate receipts select
the site; `SITE_FILTER` belongs only to a live baseline invocation.

Both candidate and public origin passed 144 HTTP assertions across eleven pages
and manifest shortcuts. Public source/version attribution, all ten startup
JS/CSS files and the service worker match the certified output exactly. The byte
detector rejected a same-size CSS mutation before its clean result was accepted.
An empty POST to the retired public diagnostics endpoint returned 410, an empty
body and `Cache-Control: no-store`.

All 24 responsive and four contrast captures were inspected, with full-resolution
phone sections checked separately. No overflow, clipped stamp, broken artwork,
unreadable phone section or runtime error was observed. Archive SHA-256 digests
match GitHub's metadata. Reviewed contact sheets, phone details and raw browser
measurement/report JSON are in `docs/evidence/studio-stack-release-2026-09-18/`.

The release engine saved the previous 100% Worker `dfa1b3d7-5f8d-4d08-9d72-2aeca9eed578`
and rollback command before promotion, then verified the provider traffic split.
Temporary previews were restored to `enabled=false, previews_enabled=false`
with provider readback. No spending cap, hosting pause or billing setting changed.
The complete machine receipt is
`docs/quality/studio-stack-release-2026-09-18.json`.

These are measurements of this changed stack candidate and its acceptance, not
proof that one dependency caused the timing improvement over an older candidate.

## Remaining queue

- Complete: Studio stack/privacy runtime candidate verification, promotion and
  public HTTP/source/asset/provider verification.
- Held: proposed legal wording approval and separate compression PR19.
- Read-only follow-up: Fevicreate stack PR2 `8ad67ff`, PITCREW stack PR2 `54fec2a`
  and Upkeep stack PR2 `db5897a` remain draft candidates. Their required browser
  acceptance remains open. Fevicreate, PITCREW, Upkeep and Fitout public hosts
  all returned 503 `DEPLOYMENT_PAUSED` in this session. No private CI rerun,
  merge or deployment was attempted for those projects.
