# Installed app recovery, 13 September 2026

Status: implementation, local tests and candidate rendering complete, release pending. This is the
studio index integration of the shared game worker/recovery fix. No claim about
physical Safari verification is made.

The previous worker deleted old immutable asset caches during activation, and
its installer saved HTML without its required styles/scripts. The old client
reloaded on controller replacement even when the current demonstration was
healthy. These are replaced by the canonical shared implementation.

- [x] Mirror canonical service-worker.ts, worker-runtime.ts, version.ts,
  asset-recovery.ts, worker-client.ts, early-source.ts and diagnostic-limits.ts
  byte for byte from the primary project.
  The source receipt records SHA-256 for each exact file.
- [x] Extend catalogue sync to verify schema, exact names, bounds and hashes of
  Circuit's /pwa-source.json before updating the mirror.
- [x] Generate public/sw.js through the existing esbuild dependency before the
  build fingerprint. The old handwritten worker is replaced by generated output.
- [x] Mount the early inline recovery in the root head. Keep the existing footer
  update indicator, with shared foreground/reconnect registration behavior.
- [x] Configure bounded migration of valid immutable assets from legacy shell-
  and assets- caches. Do not migrate their documents or unversioned artwork.
- [x] Preserve network-owned online navigation, Flight and stable artwork loads.
- [x] Bump the studio release to 0.3.1.
- [x] Add self-contained /reset without clearing saved demo or settings data.
- [x] Route bounded automatic diagnostics to canonical Circuit ingestion, with
  exact receipt forwarding and no account credentials. The early reporter stays
  active after hydration and retries only on navigation/reconnect events.
- [x] Final mirror sync after the primary diagnostic hooks are frozen.
- [x] Candidate production build and rendered browser check.
- [ ] Live mirror verification, automatic diagnostic receipt verification and deployment.

Verification: the full 106-test suite passes. An additional reset regression
reproduced denied worker access skipping accessible caches, and all three reset
tests pass after separating the cleanup operations (107 distinct passing tests
in total). Typecheck and lint pass. The candidate Next production build passes
without warnings. Proxy tests cover matching receipts, status forwarding,
cross-origin/oversize rejection, stalled-body cancellation and network retries.
The primary project's client verification has
23 focused tests and five actual hosted Chromium scenarios, including initial
missing CSS/JS, bounded retry, preserved healthy session and late CSS recovery.

Hosted Chromium checked the candidate at 1440x1000, 390x844 and 844x390: loaded
styles, version 0.3.1, no horizontal overflow, the existing update indicator,
and reset clearing Cache Storage while preserving localStorage. All three
screenshots were inspected. Evidence is in `.audit/pwa/render-results.json`
and `.audit/pwa/{desktop,phone,landscape}.png`. This rendering context blocked
service-worker registration and mocked diagnostic receipts so it did not write
production reports. Worker migration and ingestion remain separate integration
checks; physical Safari is unverified. The hosted browser and local port 3160
server were stopped after the checks.

The generated public/sw.js is removed from git tracking, ignored and generated by prebuild,
predev, pretest and the Cloudflare build wrapper. Other public assets stay tracked.
Unrelated AGENTS.md, CLAUDE.md and pnpm-lock.yaml were left untouched.

Release branch: `fix/shared-pwa-diagnostics`, based on current `origin/main`
(`b6c27cf`). Production deployment waits for the canonical Circuit source bundle
and diagnostic receipt backend to be confirmed live.
